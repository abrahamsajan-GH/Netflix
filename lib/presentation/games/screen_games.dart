import 'package:flutter/material.dart';

class ScreenGames extends StatelessWidget {
  const ScreenGames({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Screen Games')),
    );
  }
}
