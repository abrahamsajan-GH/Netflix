import 'package:flutter/material.dart';

class ScreenMyNetflix extends StatelessWidget {
  const ScreenMyNetflix({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('My Netflix')),
    );
  }
}
